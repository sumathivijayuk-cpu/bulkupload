const browserVersion = 'Chrome';
console.log(browserVersion);

function getBrowserVersion(){
    if(browserVersion=='chrome'){
        let broswerVersion = 'Edge';
        console.log(broswerVersion);
    }
}

getBrowserVersion
console.log(broswerVersion)    